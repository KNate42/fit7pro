import { Clock3, MapPin, Navigation, Phone } from 'lucide-react'
import Breadcrumbs from '../components/Breadcrumbs'
import { stores } from '../data/catalog'

export default function Stores() {
  return <main className="shell py-7 sm:py-10"><Breadcrumbs current="Наши магазины" /><div className="max-w-2xl"><span className="eyebrow"><MapPin size={15} />Петропавловск</span><h1 className="section-title">4 магазина рядом с вами</h1><p className="mt-5 text-sm leading-7 text-slate-600">Забирайте заказ в удобной точке или заходите, чтобы выбрать спортивное питание на месте.</p></div><div className="mt-10 grid gap-4 lg:grid-cols-[.9fr_1.1fr]"><div className="grid gap-3">{stores.map((store, index) => <article key={store.name} className="rounded-[22px] border border-line p-5 transition hover:border-lime hover:shadow-card"><div className="flex gap-3"><span className="grid h-9 w-9 shrink-0 place-items-center rounded-xl bg-mist text-moss">{index + 1}</span><div><h2 className="text-sm font-extrabold">{store.name}</h2><div className="mt-3 space-y-2 text-xs leading-5 text-slate-600"><p className="flex gap-2"><MapPin size={15} className="shrink-0 text-moss" />{store.address}</p><p className="flex gap-2"><Clock3 size={15} className="shrink-0 text-moss" />{store.hours}</p><p className="flex gap-2"><Phone size={15} className="shrink-0 text-moss" />{store.phone}</p></div><a target="_blank" rel="noreferrer" href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(store.address + ', Петропавловск')}`} className="mt-4 inline-flex items-center gap-1.5 text-xs font-bold text-moss hover:underline"><Navigation size={14} />Построить маршрут</a></div></div></article>)}</div><div className="relative min-h-[440px] overflow-hidden rounded-[28px] bg-[#e7f4ea]"><div className="absolute inset-0 opacity-30" style={{ backgroundImage: 'linear-gradient(#0d9b5120 1px, transparent 1px), linear-gradient(90deg, #0d9b5120 1px, transparent 1px)', backgroundSize: '38px 38px' }} /><div className="absolute inset-x-[15%] top-[15%] h-[2px] rotate-[18deg] bg-lime/40" /><div className="absolute inset-x-[24%] top-[55%] h-[2px] -rotate-[30deg] bg-lime/40" />{[[28,30],[66,27],[47,55],[72,70]].map(([left, top], index) => <div key={index} style={{ left: `${left}%`, top: `${top}%` }} className="absolute"><div className="grid h-10 w-10 place-items-center rounded-full border-4 border-white bg-lime text-xs font-extrabold text-white shadow-card">{index + 1}</div></div>)}<div className="absolute bottom-5 left-5 rounded-xl bg-white/90 p-3 text-xs font-semibold text-moss shadow-card backdrop-blur">Интерактивная карта будет подключена на этапе разработки</div></div></div></main>
}
